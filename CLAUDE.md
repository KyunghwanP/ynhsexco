# BIO-DATA 탐사대 — 검진 입력 시스템

과학축전 부스에서 쓰는 단일 파일 웹앱. 방문객 문진을 받고, 측정값을 입력하고,
QR이 박힌 번호표를 뽑고, 본인 결과를 조회하게 한다.

**작업 전에 이 문서를 끝까지 읽을 것.** 아래 "이미 버린 선택지"에 적힌 것을
다시 제안하면 안 된다. 전부 이유가 있어서 뺐다.

---

## 1. 무엇을 만드는가

| | |
|---|---|
| 행사 | 과학축전, 대구 엑스코 |
| 신청 주제 | BIO-DATA 탐사대: 심장의 전기 신호를 데이터 모델로 읽어내기 |
| 부스 컨셉 | 건강검진센터 |
| 규격 | 6m(가로) × 3m(세로) × 2m(높이), **앞면만 개방** |
| 기간 | 2일 |
| 목표 | 시간당 30명, 총 300명 |
| 대상 | **초등학생~성인** (보호자 동반 많음) |
| 예산 | 180만원 |
| 운영 | 학생 최소 4명 (동시 3 + 유동 1) |
| 기기 | **노트북 2대 + 학생 폰**. 태블릿은 쓰지 않는다 |
| 배포 | GitHub Pages — https://kyunghwanp.github.io/ynhsexco/ |

### 기기 구성 — 노트북 2대 + 폰

| 자리 | 기기 | 왜 |
|---|---|---|
| 실시간 보드 | **노트북 1** | 화면이 크고 거치대가 필요 없다. `?board=a` |
| 결과·굿즈 + 인쇄 | **노트북 2** | 라벨 프린터 USB, A4 결과지. 붙박이라 깊이가 문제 안 된다 |
| 접수·문진 | **학생 폰** | 대기줄에서 들고 다녀야 한다 |
| 스테이션 입력 | **학생 폰** | 서서 숫자 두세 개. 노트북은 깊이 30cm를 먹어 측정 장비와 겹친다 |

번호표를 키로 쓰므로 기기 종류를 가리지 않는다. 브라우저만 있으면 된다.

**노트북에서는 QR 스캔이 사실상 안 된다.** 웹캠 각도가 나빠서다. 노트북에서는
번호를 직접 치고(`A012`처럼 전체 번호도 받는다), QR 스캔은 폰에서만 쓴다.

부스 동선: 접수·문진 → 검사존(자유 순환) → 순발력 → 결과·굿즈.
검사존은 순서를 강제하지 않는다. 번호표를 쥐여주고 빈 곳부터 이용하게 한다.

---

## 2. 지금 코드 상태

파일 셋. 빌드 도구 없음. 라이브러리는 전부 인라인.

| 파일 | 누가 쓰나 | 무엇이 들어 있나 |
|---|---|---|
| `index.html` | **학생만** | 문진 · 측정 입력 · 기록 · 설정 · 실시간 보드 |
| `result.html` | **방문객** | 본인 결과 조회. 읽기만 한다 |
| `staff.html` | **운영 학생** | 교대표 · 내 시간 · 매뉴얼 · 명단. **검진 데이터와 무관** |

**방문객이 여는 페이지에는 부스 기능이 아예 없다.** 예전에는 한 파일이라
탭을 숨겨도 눌러서 들어갈 수 있었고, 실제로 방문객이 가짜 접수를 만들어
서버에 올릴 수 있었다. 파일을 나눠서 그 경로를 없앴다.

`result.html`은 `setDoc`을 들여오지도 않는다. 쓰기 함수가 없는 것이 그 자체로
보호 장치다. QR 생성기(`qrcode`)와 스캐너(`jsQR`)도 필요 없어 빠졌다 — 21KB다.

**`staff.html`은 `records` 컬렉션을 건드리지 않는다.** `staff/roster` 문서 하나만
읽고 쓴다. 방문객 기록과 아무 관계가 없어서 `judge`·`lifeAge`·`ekgChart` 같은
것을 하나도 안 쓴다 — 그래서 아래 "두 파일에 똑같이 있는 블록"이 셋으로
늘어나지 않았다. 겹치는 것은 `escapeHtml` 하나뿐이다.

### 탭 구성
- **문진** — 6단계 설문 → 접수번호 발급 + QR + 판정 + 라벨 인쇄
- **측정 입력** — 번호 조회 또는 QR 스캔 → 항목별 입력.
  담당 스테이션(전체 / 혈압·EKG / 악력·우세눈 / 순발력)을 고르면 그 항목만 뜬다.
  EKG를 "측정함"으로 두면 PASCO CSV를 넣는 칸이 열린다.
  파일 고르기 · 끌어다 놓기가 기본이고, 붙여 넣기는 접어 두었다.
  조회된 사람 옆 **결과지** 버튼으로 A4를 뽑는다.
  심박수·악력에는 **"예상은?" 칸**이 앞에 있다. 재기 전에 먼저 물어보고 넣는다
- **기록** — 목록, 카운터, CSV 내려받기
- **설정** — 기기 코드, 일차, 라벨 인쇄 옵션, 백업
- **결과** — `result.html?r=A001&k=…`. 판정 + 심전도 + 참여자 중 내 위치.
  `index.html?r=` 로 들어와도 `result.html`로 넘긴다(예전에 뽑은 번호표 대비)
- **실시간 보드** — `?board=a` (검사존·산점도 4개) / `?board=b` (입구·현황 6개) / `?board=1` (전부).
  8초마다 자동 전환. 화면을 누르거나 → ← 로 넘기고, `t` 로 표 보기.
  위쪽 막대가 다음 화면까지 남은 시간이고, 넘어갈 때 짧게 사라졌다 올라온다.
  새 측정이 도착하면 그 점을 3초 강조한다

### 인라인 라이브러리
- `qrcode-generator` 2.0.4 (minify) — QR 생성. 전역 `qrcode`
- `jsQR` 1.4.0 (minify) — QR 인식. 전역 `jsQR`

**인터넷 없이 동작하게 하려고 일부러 인라인했다.** CDN으로 바꾸지 말 것.

### 저장
- **localStorage** — 항상 먼저 쓴다. 서버가 죽어도 데이터가 남는다
- **Firestore** (`ynhs-exco` 프로젝트, `records` 컬렉션) — 문서 ID = 접수번호
- 병합은 `upd`(epoch ms)가 최신인 쪽이 이긴다
- **`stats/live` 문서** — 이름·나이·번호 없이 값만 모은 분포(악력·반응·심박·생활습관나이).
  부스 기기가 저장할 때마다 3초 묶어서 갱신한다
- 오프라인 캐시(`persistentLocalCache`) 켜져 있음
- 헤더 오른쪽에 상태 표시: `동기화됨` / `오프라인` / `로컬 전용`

**`result.html`은 컬렉션 전체를 구독하지 않는다.** 자기 문서 하나(`FB.one`)와
`stats/live`만 읽는다. 방문객 휴대폰에 참여자 300명의 이름·나이·혈액형이
넘어가지 않게 하려는 것이다. `onSnapshot(col)` 하나로 합치지 말 것.

### 핵심 함수
| 함수 | 역할 |
|---|---|
| `saveAll(one?)` | 로컬 저장 + 서버 업로드. 인자 없으면 전체 |
| `saveLocal()` | 로컬만 |
| `mergeRemote(list)` | 서버 목록을 로컬과 병합. 변경 여부 반환 |
| `touch(r)` | `upd` 갱신 |
| `judge(r)` | 수면/활동/스트레스 3단계 판정 → 키캡 색상 |
| `lifeAge(r)` | `{diff, pct, cause}`. 나이에 비례한 차이와 주원인 |
| `cm2ms(cm)` | 자막대 낙하거리 → 반응시간. `t=√(2d/g)` |
| `qrFor(id,name)` | QR SVG. 내용은 `result.html?r=<번호>&k=<열쇠>` |
| `nameKey(id,name)` | 번호+이름에서 만든 8자리 열쇠. **result.html에도 같은 함수가 있다** |
| `printLabel(r)` | 새 창 띄워 인쇄 |
| `findRec(raw)` | 번호 또는 QR URL 문자열로 조회 |
| `stationGroups()` | 담당 스테이션의 측정 항목만 반환 |
| `harvestFields()` | 입력칸 값을 기록으로 거둠. **화면을 다시 그리기 전에 반드시 호출** |
| `buildStats()` | 이름 없는 분포 집계 생성 |
| `publishStats()` | 3초 묶어 `stats/live`에 올림 |
| `rankIn(arr,v,dir)` | 분포 안 백분위. 표본 3 미만이면 `null` |
| `parseEkg(text)` | 머리글에서 시간·전압·심박 열을 찾아 읽는다 → `{v, sec, hz, devBpm}` |
| `cleanEkg(v,hz)` | 60Hz 잡음 · 기저선 흔들림 · 고주파 제거 |
| `bestWindow(x,hz,sec)` | 진폭 대비 잔떨림이 가장 작은 구간 |
| `prepEkg(v,hz)` | 위 셋을 묶어 `{wave, raw, sec, bpm}`. R파가 위를 향하게 자동 반전 |
| `rPeaks(w)` / `ekgBpm(w,sec)` | R파 위치 / 파형에서 센 심박 |
| `ekgChart(w,opt)` | 심전도 모눈 위에 파형. `zoom`은 한 박동 확대, `annotate`는 P·QRS·T |
| `ekgLooksReal(w)` | 심전도처럼 생겼는지. R파 개수·간격·높이를 본다 |
| `refWave()` | **보기용 표본 파형.** P·QRS·T 설명에만 쓴다. 누구의 기록도 아니다 |
| `printSheet(r)` | A4 검진 결과지. 새 창을 띄워 인쇄 |
| `boardScreens()` | 보드 화면 목록. `?board` 값에 따라 A/B/전부 |
| `bScatter/bGroups/bHist` | 산점도 · 그룹 점분포 · 히스토그램 (인라인 SVG) |
| `eyeHand()` / `bMatch(d)` | 더 센 손 × 우세눈 짝 · 큰 숫자 + 2×2 격자 (탐사 질문 7번) |
| `flashPoint(id)` | 방금 들어온 기록을 3초 강조 |
| `guessLine(예상,실제,단위)` | "예상 25kg · 실제 31kg — 6kg 차이. 꽤 비슷해요" |
| `AGE_BANDS` / `bandRecords()` | 또래 구간(초저·초고·중고등·어른)과 구간별 최고 기록 |

---

## 3. 데이터 구조

```js
{
  id: "A001",            // 문서 ID이자 접수번호. dev + 3자리
  seq: 1, dev: "A", day: 1,
  at: "2026-10-18T...",  // 접수 시각
  upd: 1760000000000,    // 마지막 수정 (병합 기준)

  // 문진
  name: "박○○", age: 14, sex: "남", blood: "O", glass: "씀",
  symp: ["두통","눈의 피로"],          // 또는 ["해당 없음"]
  sleep: 7, fall: 1, wake: 1,          // 시간 / 입면 0~2 / 기상 0~2
  bfast: 0, sweet: 2, water: 1,        // 0이 가장 좋음
  exer: 1, sit: 1, screen: 2,
  m1: 2, m2: 3, m3: 1,                 // 표정 척도 0(좋음)~4(나쁨)

  // 측정 — 값이 "skip"이면 미측정
  m: {
    hr: {bpm: 78},
    grip: {l: 30, r: 34},
    react: {a: 18, b: 15, c: 16},
    reactMs: 175,                      // 3회 중 최고, 자동 계산
    eye: "R",                          // 우세눈
    ekg: "Y",
    ekgWave: [0,12,…],                 // 다듬은 파형 600점, 0~1000 정수
    ekgRaw:  [0,15,…],                 // 다듬기 전 600점. 둘 합쳐 약 4.8KB
    ekgSec: 5.0,                       // 골라 담은 구간 길이 (전체가 아니다)
    ekgBpm: 71,                        // PASCO 심박 열이 있으면 그 값, 없으면 계산값
    bp: {s: 118, d: 76},               // 선택 항목
    // 예상값. 재기 전에 본인이 말한 숫자다. 실제 칸과 나란히 저장된다
    // grip: {g: 25, l: 30, r: 34}  ·  hr: {g: 80, bpm: 78}
  }
}
```

**모든 측정 항목은 "skip" 저장이 가능해야 한다.** 저학년이 거부하는 일이
반드시 생기고, 입력이 막히면 현장에서 줄이 선다.

---

## 4. 판정 기준

### 수면 권장량 (나이별)
초등 이하 10h / 중고등 9h / 성인 8h — `sleepNeed(age)`

### 생활습관 나이
수면량·운동량·화면 시간·아침밥·기분 5항목에 각 −2 / 0 / +2점.
합계(−10~+10)에 **2.2%**를 곱해 비율을 내고, 그 비율만큼 실제 나이를 늘리거나 줄인다.
표시는 **`22세 (+4년)`** — 절대 나이와 차이를 같이 보여준다.

**고정 ±8년을 더하지 않는 이유:** 어린 학생이 실제 나이 밑으로 내려가 음수가 된다.
7세 − 8 = **−1세**다. 비율로 하면 7세는 5세까지만 내려간다.

| 나이 | 가장 좋을 때 | 가장 나쁠 때 |
|---|---|---|
| 7세 | 5세 (−2년) | 9세 (+2년) |
| 12세 | 9세 (−3년) | 15세 (+3년) |
| 18세 | 14세 (−4년) | 22세 (+4년) |
| 45세 | 35세 (−10년) | 55세 (+10년) |
| 60세 | 47세 (−13년) | 73세 (+13년) |

**년수 상한은 두지 않는다.** 비율이 곧 상한이라 규칙이 하나뿐이고,
0.78을 곱하는 것이라 어떤 나이에서도 0 밑으로 내려가지 않는다.
예전의 ±8년 제한은 초중학생에게는 아예 걸리지 않아(36세부터만 걸렸다)
규칙만 둘로 늘리는 셈이었다.

**가중치에 근거는 없다.** 임의로 만든 값이므로 화면에 "재미로 계산한 지수"를
반드시 표기한다. 이 문구를 지우지 말 것.

### 용어는 "이"가 붙는 말로 통일한다
`수면량` `운동량` `화면 시간` `아침밥` `기분` — 전부 받침이 있어 조사가 **"이"** 하나로 끝난다.
판정 3항목도 `수면량` `활동량` `스트레스량`이다.
`이(가)` 같은 어색한 표기를 다시 넣지 말 것. 말을 바꾸려면 받침 있는 말로 고를 것.
`ADVICE`의 열쇠는 `lifeAge()`의 `names`와 **글자까지 같아야** 처방이 붙는다.

### 반응속도
자막대 낙하거리 → `t = √(2d/g)`, g=9.8. 15cm ≈ 175ms, 20cm ≈ 202ms.

---

## 5. 탐사 질문 (부스 전면 게시)

1. 잠을 적게 자면 반응속도가 느려질까?
2. 운동량이 많으면 악력이 셀까?
3. 스트레스가 높으면 안정 시 심박수가 빠를까?
4. 주손과 비주손의 악력 차이는 몇 %일까?
5. 나이가 많을수록 반응속도가 빠를까?
6. 악력은 몇 살부터 급격히 늘어날까?
7. 더 센 손과 우세눈은 같은 쪽일까?

**5·6·7번은 확실히 결과가 나온다.** 5·6번은 연령 범위가 넓어서 효과 크기가 크고,
7번은 한 사람에게서 둘을 같이 재는 짝지은 비교라 표본이 적어도 답이 나온다.
1·3번은 표본 300명, 자기보고 문진, 거친 측정 때문에 상관이 안 보일 가능성이 높다.

이건 실패가 아니다. "왜 안 보였을까"를 한계와 함께 보고하는 것이 목표다.
억지로 상관을 만들어내는 쪽으로 코드를 짜지 말 것.

---

## 6. 이미 버린 선택지 — 다시 제안하지 말 것

| 항목 | 이유 |
|---|---|
| **열전사 아크릴 각인 / 큐브 조립** | 신청서 원안이었으나 제외. 전기 용량·화상 관리·공간 부담. 결과지+키캡으로 대체 |
| **처방 캡슐 키링** | 7종 쪽지 인쇄·재단·420개 수작업. 준비 시간 대비 효과 낮음. 처방 문구는 결과지 하단 인쇄로 흡수 |
| **대형 모니터 / 스탠바이미** | 현장 대여 없음. 55인치 운반 비현실. 개인 장비는 파손 리스크. **실시간 보드는 노트북** |
| **벽면 대형 인쇄물** | 발주·부착 확인·당일 시공 부담. X배너 + 아크릴 스탠드로 대체 |
| **채혈 혈액형 판정** | 감염·의료폐기물·무면허 의료행위. 혈액형은 자가 기입만 |
| **키·몸무게 수집** | 이탈 발생 지점. 탐사 질문 어디에도 안 쓰임 |
| **산소포화도(SpO2) 표시** | 오측정이 흔하고(손 참·움직임) 수습 불가. 기기는 쓰되 **BPM만 표시** |
| **BEPSI-K 스트레스 척도** | 성인용이라 초등학생이 답할 수 없음. 표정 아이콘 3문항으로 대체 |
| **일회용 ECG 전극 재사용** | 하이드로겔이 마르면 신호가 못 쓰게 나옴. 위생 이전에 작동 불가 |
| **전극을 손으로 눌러 대기** | 누르는 사람 전위·근전도·떨림이 섞임. 붙이거나 쥐거나 둘 중 하나 |
| **천 벨크로 팔찌 전극** | 천은 알코올 소독이 안 됨. **구리 파이프 손잡이**로 확정 |
| **뇌파(EEG)** | μV 단위라 전시장에서 잡히지 않음. 장비도 비쌈 |
| **연령대별 문진 분기** | 두 벌 만들면 탐사 데이터가 쪼개져 같은 그래프에 못 찍음. 한 벌 + 나이를 변수로 |

---

## 7. 알려진 제약 — 고치려 들기 전에 읽을 것

### iframe 안에서 인쇄가 안 된다
claude.ai 아티팩트처럼 샌드박스 iframe에 들어가면 `window.print()`가 조용히
무시된다. 그래서 `printLabel()`은 **새 창을 띄워 거기서 인쇄**한다.
GitHub Pages에서는 정상 동작한다.

### 카메라는 https에서만 열린다
QR 스캔은 secure context가 필요하다. GitHub Pages(https)는 되고,
노트북 로컬 서버(`http://192.168.x.x`)는 **안 된다**.
로컬 서버로 옮기자는 제안이 나오면 이 비용을 반드시 언급할 것.
번호 직접 입력은 항상 가능하게 남겨둔다.

### 결과지는 A4 두 쪽에 꽉 차 있다
앞면(결과)과 뒷면(패널 5장 + 심전도 읽는 법)이 **각각 정확히 297mm**다.
줄을 하나라도 늘리면 세 쪽으로 넘어가 양면 인쇄가 어긋난다.
내용을 더할 때는 `.page` 높이를 브라우저에서 재 보고 다른 곳을 그만큼 줄일 것.

`.foot`은 `margin-top:auto`로 흐름 안에 있다. `position:absolute`로 바꾸면
본문을 덮으면서도 높이 측정에는 안 잡혀서 겹침을 놓친다.

### EKG — 실제 PASCO 파일로 확인한 것

PS-3236 내보내기는 열이 다섯이다.
`일시 / 시간 (s) / 전압 (mV) / 심박동율 (박동/분) / SmoothedVoltage (mV)`

- 맨 앞이 **날짜 문자열**이다. 줄에서 숫자만 긁어 첫 번째를 시간으로 쓰면
  `2026`이 시간이 된다. 머리글에서 열을 찾아야 한다
- **심박동율은 전압이 빈 행에만** 실려 온다. 채널마다 표본 주기가 달라서다.
  전압이 빈 행을 건너뛰기 **전에** 이 열을 읽어야 한다
- `SmoothedVoltage`가 아니라 **원본 `전압`을 읽는다.** 다듬기는 우리가 한다
- 한글 윈도에서 내보내면 **EUC-KR**로 저장되기도 한다. UTF-8로 읽어 깨지면
  EUC-KR로 다시 읽는다(`readTextSmart`). 머리글이 깨지면 열 이름을 못 찾으므로
  이 대비가 없으면 조용히 엉뚱한 열을 읽는다

실측한 신호(2026-09-17, 200Hz, 11.3초, 14박동):

| | |
|---|---|
| 60Hz 전원 잡음 | 신호 성분의 **2배**. 들쭉날쭉의 주범 |
| 기저선 흔들림 | 0.26mV (신호 진폭의 12%) |
| 실제 R파 높이 편차 | 1.2배뿐 |

**화면에서 R파가 뒤로 갈수록 커져 보였던 것은 신호 탓이 아니라 다운샘플
에일리어싱이었다.** 11초를 480점으로 줄이면 43Hz가 되어 뾰족한 R파가 표본에
걸렸다 말았다 한다. 그래서 **가장 깨끗한 5초만 골라 600점**으로 담는다(120Hz).
이 창을 늘리거나 점 수를 줄이면 그 현상이 돌아온다.

### 파형을 다듬는 선

`cleanEkg()`가 하는 일은 **걷어내는 것**이다. 임상 심전도 모니터와 같은 발상이다.

1. 전원 주파수 한 주기 길이의 이동평균 → 60Hz 제거
2. 0.7초 이동중앙값을 빼서 기저선 흔들림 제거
3. 45Hz 위 억제

측정 결과 60Hz는 98% 줄고 신호(3Hz)는 97% 남았다. 심박은 PASCO 자체 값과 일치했다.

**심전도처럼 안 생긴 기록은 확대하지도 라벨을 붙이지도 않는다.**
`ekgLooksReal()`이 R파 개수·간격의 고름·솟은 높이를 본다. 통과 못 하면
`zoom`과 `annotate`를 꺼서 전체 파형만 그리고, 설명 문구도 바뀐다.
전극이 덜 붙은 기록에 P·QRS·T 딱지를 붙이면 없는 것을 만들어내는 것이다.

### P·QRS·T는 본인 파형이 아니라 보기용 표본에 붙인다

부스에서는 전극이 덜 붙거나 팔을 움직여서 **`ekgLooksReal()`을 통과 못 하는
기록이 꽤 나온다.** 본인 파형에 라벨을 붙이는 방식이면 그런 사람은 설명을
아예 못 받는다. 그래서 읽는 법은 `refWave()`가 만든 **보기용 표본**에 붙인다.

- 결과지 뒷면 6번 패널과 결과 조회의 "심전도 읽는 법"이 표본을 쓴다
- 본인 파형은 앞면·위쪽에 **라벨 없이 있는 그대로** 둔다
- 제목 옆에 **`보기 그림`** 딱지를 붙이고, 그림 위에 한 줄
  **"교과서에 나오는 그림입니다. 내 기록이 아니에요."**를 굵게 적는다.
  **왜 표본을 쓰는지까지 설명하지 않는다** — 길면 안 읽고, 안 읽으면 없는 것과 같다.
  다만 딱지와 이 한 줄은 지우지 말 것. 둘 다 없으면 표본이 본인 심장으로 읽힌다
- `refWave()`는 같은 박동을 3개 이어 붙인다. `rPeaks`·`ekgLooksReal`이
  R파를 세려면 3박동이 필요해서다. 개수를 줄이면 표본이 확대되지 않는다

**표본을 본인 데이터에 섞지 말 것.** 저장(`m.ekgWave`)·심박 계산·분포 집계
어디에도 `refWave()`가 들어가면 안 된다. 그리기에만 쓴다.

**확대도의 가로세로 비는 SVG 에 직접 박는다**(`style="aspect-ratio:W/H"`).
CSS 의 `height:auto` 에 맡기면 인쇄 맥락에서 상자만 커지고 그림이 가운데
눌린 채로 나오는 경우가 있다.

**여기서 더 나가지 말 것.** 여러 박동을 평균내 한 박동을 만들거나, 본인 파형에
교과서 모양을 섞거나, 없는 P파를 그려 넣는 것은 데이터를 만들어내는 것이다.
(바로 위의 표본 파형은 **본인 파형과 따로 그리고 아니라고 밝히므로** 다른 얘기다.)
8장의 선을 넘는다. 원본(`ekgRaw`)을 함께 저장하고 화면에 "다듬기 전"을 같이
보여주는 것도 그래서다. 이 비교를 없애지 말 것.

### 실시간 보드에서 지킨 것

**1~2m 떨어져 보는 화면이고 마우스가 없다.** 그래서 일반적인 차트 규칙 중
두 가지를 다르게 했고, 나머지는 그대로 지켰다.

- **hover 툴팁을 넣지 않았다.** 대신 방금 측정한 점에 직접 라벨을 붙이고,
  `t` 키로 같은 자료를 표로 볼 수 있게 했다
- **한 화면에 그래프 하나만.** 4분할하면 멀리서 아무것도 안 읽힌다

지킨 것:
- 축은 하나다. 두 측정값을 한 그래프에 겹치지 않는다
- 순서 있는 3단계(운동량·기분)는 **한 색조의 명도 단계**로만 칠한다.
  `ORD_LIGHT`/`ORD_DARK`는 dataviz 검증기(`--ordinal`)를 통과한 값이다.
  임의로 바꾸면 다시 검증할 것
- 그룹 비교는 **큰 채색 막대가 아니라 점 분포 + 평균선**이다. 막대로 칠하면
  블록이 점을 덮어 분포가 안 보인다
- 히스토그램 눈금은 막대 **가운데가 아니라 구간 경계**에 찍는다.
  가운데에 두면 그 값이 구간의 시작인지 중앙인지 알 수 없다
- 표본 5명 미만이면 그래프를 그리지 않고 "자료를 모으는 중"을 띄운다

산점도에 회귀선은 넣지 않았다. 구간 평균만 이어 보여준다.
5장의 "억지로 상관을 만들어내지 말 것"에 걸리기 때문이다.

### 예상 먼저 묻기

심박수·악력은 **재기 전에 "얼마나 나올 것 같아?"를 묻고** 그 숫자를 먼저 넣는다.
`m.grip.g` / `m.hr.g` 다. 다른 칸과 똑같이 저장되므로 따로 손댄 machinery는 없다.

- 재고 나면 그 자리에서 `guessLine()`이 차이를 보여준다
- 결과지와 결과 조회에도 "내 예상은 얼마였어요"가 남는다
- `buildStats()`가 **실제 − 예상**을 모아 보드 화면 하나를 만든다
  ("사람들은 자기 악력을 얼마나 잘 맞출까?")
- `gripOf()`는 `g.l`과 `g.r`만 본다. **예상값 `g.g`가 최고 악력에 섞이면 안 된다**

예측 → 측정 → 비교가 이 부스가 가르치려는 것이고, 틀리는 순간이 제일 재밌다.

### 또래 기록판은 또래끼리만 겨룬다

어른과 한 줄에 세우면 초등학생은 악력·반응속도에서 **늘 바닥**이다.
그래서 `?board=b`의 기록판은 `AGE_BANDS`로 넷으로 나눈다.
이름은 내걸지 않고 **번호만** 쓴다.

`AGE_BANDS`는 표시용이지 문진을 나누는 것이 아니다.
6장의 "연령대별 문진 분기 금지"와는 다른 얘기다. 데이터는 한 벌 그대로다.

### 번호만 바꿔서 남의 결과를 열 수 없다

QR에 번호와 함께 **이름에서 만든 8자리 열쇠**(`nameKey`)를 담는다.
`result.html`은 서버에서 읽은 기록의 이름으로 열쇠를 다시 만들어 맞춰 본다.

- QR로 들어오면 열쇠가 맞으므로 **아무것도 입력하지 않고 바로 열린다**
- 열쇠가 없거나 안 맞으면 **"번호표의 QR을 찍어 주세요"만 뜬다**
- 이름을 그대로 주소에 넣지 않은 이유: 번호표 QR을 사진만 찍어도 이름이 읽힌다

**이름을 직접 치는 대체 입구를 두지 말 것.** 한 번 넣었다가 뺐다.
방문객에게 타이핑을 시키는 순간 QR의 의미가 없어지고, 번호와 이름을
바꿔 가며 넣어 볼 창구를 열어 주는 셈이다. QR이 안 읽히면 접수처에서
부스 기기로 확인하면 된다.

**왜 안 열리는지 알려주지 않는다.** 번호가 없는 것인지 열쇠가 틀린 것인지
구분해 주면, 번호를 하나씩 넣어 보며 있는 번호를 찾아낼 수 있다.

**이것도 사고를 막는 장치이지 암호가 아니다.** Firestore 규칙이 열려 있어
마음먹은 사람은 SDK로 직접 읽을 수 있다. 진짜로 막으려면 로그인이나 App Check가
필요한데 이 규모에는 과하다. 끝나고 규칙을 잠그는 것이 실효가 있다.

### 두 파일에 똑같이 있는 블록

`result.html`의 "공유 블록"은 `index.html`에서 그대로 떠온 것이다.

`escapeHtml` `nameKey` `sleepNeed` `judge` `lifeAge` `signedYears` `ecgSvg`
`rPeaks` `ekgLooksReal` `refWave` `ekgChart` `gripOf` `rankIn` `GRADE` `GCOLOR` `ECG_PATH`
`EKG_NOTE` `EKG_CLEAN_NOTE` `MIN_POOL`

**판정 기준이나 파형 그리기를 고칠 때 한쪽만 고치면**, 번호표·결과지에 찍힌
값과 방문객이 보는 값이 **조용히 달라진다.** 오류도 안 난다.
고친 뒤에는 두 파일에서 해당 함수를 꺼내 글자까지 같은지 비교할 것.

### 상태가 "오프라인"에 멈춰 있으면

`onSnapshot`은 기본적으로 **문서 내용이 바뀔 때만** 콜백을 부른다.
오프라인 캐시를 켜 두면 첫 스냅샷이 캐시에서 오고(`fromCache: true` → "오프라인"),
서버가 같은 내용으로 확인해 줄 때는 metadata만 바뀌므로 **콜백이 오지 않는다.**
그래서 기록이 없는 새 기기는 연결이 멀쩡해도 "오프라인"에 멈춘 채 안 바뀐다.

`onSnapshot(col, { includeMetadataChanges: true }, …)`로 열어야 한다.
이 옵션을 빼면 증상이 그대로 돌아온다.

### 파형 글자는 SVG 밖에 둔다
전체 파형은 `preserveAspectRatio="none"`으로 가로를 늘여 그린다. 그 안에 `<text>`를
넣으면 글자도 같이 늘어나 찌그러진다. 그래서 초 표시 같은 것은 HTML 설명문으로 뺐고,
P·QRS·T 라벨이 필요한 확대도만 비율을 지켜(`uni`) 그린다.

### 라벨 규격
- 프린터: **브라더 QL-820NWB** (300×600dpi, 자동 커터, USB/유선랜/무선/블루투스)
- 용지: **DK-22212** 62mm 연속 **필름**, 롤 15.2m
- 현재 설정: `@page { size: 62mm 35mm }`, 길이 35mm면 롤당 약 434장
- **인쇄 가능 폭은 62mm보다 좁다** (실제 58~59mm). 오른쪽 QR이 잘릴 수 있음
- **필름이라 광택이 있다.** 조명 반사로 QR 스캔이 실패할 수 있음 → 실패하면
  DK-22205(62mm 무광 종이)로 교체
- 드라이버에서 62×35mm 사용자 정의 용지를 등록해야 브라우저 인쇄와 맞는다
- 크롬 `--kiosk-printing` 옵션이면 대화상자 없이 바로 뽑힌다

### 프린터 연결
블루투스로 붙이면 전용 앱을 거쳐야 해서 자동 인쇄가 안 된다.
**노트북에 USB로 물리는 것이 확실하다.** 폰에서 접수하면 Firestore를 통해
노트북이 받아서 인쇄하는 구조.

### 운영 교대표 — `staff.html`

운영 학생 20명. 1일차 10~18시(8칸) · 2일차 10~16시(6칸) · 1시간 슬롯 ·
자리 6개(접수·문진 / 혈압·EKG / 악력·우세눈 / 순발력 / 결과·굿즈 / 유동) = **84칸**.

**격자는 시간대별이고, "덩어리"는 그 위에 얹은 운영 단위다.** 둘은 다른 것이다.

| | |
|---|---|
| 격자 | 1시간 칸. 누가 몇 시에 어느 자리인지 |
| 덩어리 | 1일차 오전·오후 / 2일차 오전·오후 넷. 기본값은 10\~14 · 14\~18 · 10\~13 · 13\~16 |

덩어리를 쓰는 곳은 둘뿐이다.

1. **명단에서 "언제 나오나"를 받는 단위.** 날 단위로 받으면 "1일차 오후만 가능"을
   적을 수가 없다. 체크박스가 사람마다 넷이다
2. **격자의 구분줄.** 오전·오후가 어디서 갈리는지 보이고,
   그 덩어리에 나올 수 있는 사람이 몇 명인지 줄에 같이 적는다

덩어리는 설정에서 날마다 더하고 지울 수 있다. 시간 단위로 잘게 쓰고 싶으면
덩어리를 여러 개 만들면 된다 — 개념을 둘로 늘리지 않으려고 이렇게 했다.

- 가로축이 **자리**다. 학생 기준으로 짜면 빈 자리를 눈으로 세어야 한다
- **유동은 필수가 아니다**(`need`=0). 점심 교대와 잠깐 자리 비우는 것을 여기로 흡수한다
- **자동 채우기는 정답이 아니라 빈 격자를 안 보게 하는 용도**다.
  84칸을 손으로 누르게 하면 이 페이지는 안 쓰게 된다.
  `canWork()`로 **안 나오는 덩어리에는 절대 안 넣는다**
- `fillSpare()`가 한 번도 안 들어간 사람을 **유동 자리에 넣는다.**
  필수 자리가 5개뿐이라 20명이면 남는 사람이 반드시 생기는데,
  격자에 이름이 없으면 그 사람은 자기가 언제 오는지 알 수가 없다
- 채운 뒤 `balancePass()`가 고르게 편다. 2칸씩 묶어 세우면 덩어리가 몰려서
  **같은 조건인데 근무가 몇 시간씩 벌어진다.** 이 뒷정리를 빼면 증상이 돌아온다.
  옮길 때도 덩어리를 지킨다
- 근무 시간 균형은 **나오는 덩어리 조합이 같은 사람끼리만** 견준다.
  많이 나오는 사람이 많이 서는 건 당연하다

### 검사존 자리를 이렇게 묶은 이유

| 자리 | 항목 | 왜 여기 |
|---|---|---|
| 혈압·EKG | `bp` `hr` `ekg` | 둘 다 **앉혀서 가만히** 있게 하는 측정이다. 안정 시간을 한 번만 쓰면 된다 |
| 악력·우세눈 | `grip` `eye` | 둘 다 **왼쪽·오른쪽**을 본다. 더 센 손과 우세눈을 그 자리에서 견줄 수 있다 |
| 순발력 | `react` | 자막대 3회. 기다리는 사람은 캐치스틱 |

**심박은 혈압계에서 같이 나온다.** 그래서 손가락 측정기는 백업이다.
다만 **혈압을 건너뛰면 심박까지 사라진다** — 탐사 질문 3번(스트레스↔심박)이 통째로 빈다.
커프를 싫어하는 저학년에게는 손가락 측정기로 심박만 재게 할 것.

**악력계는 3대다.** 셋이 동시에 재니까 그 자체가 대결이 되고, 이 자리 처리량이 세 배가 된다.
대신 **한 사람의 양손은 반드시 같은 악력계로** 재야 한다. 기계마다 값이 조금씩 달라서,
좌우를 다른 기계로 재면 **탐사 질문 4번(주손과 비주손 차이 %)이 손 때문인지 기계 때문인지
구분되지 않는다.** 이 한 줄이 매뉴얼의 "이것만은" 맨 위에 있다.

**캐치스틱은 측정이 아니다.** 떨어지는 걸 잡는 완구이고 숫자가 안 나온다.
순발력 자리에서 **줄을 흡수하는 용도**로 쓰고 기록에는 넣지 않는다.

### 탐사 질문 7번은 그래프가 아니라 큰 숫자다

**더 센 손과 우세눈이 같은 쪽인지**는 답이 비율 하나다. 산점도도 히스토그램도
맞지 않아서 `bMatch()`가 **큰 숫자 + 2×2 격자**로 그린다.
1~2m 떨어져 보는 화면에서는 축과 눈금보다 숫자 하나가 먼저 읽힌다.

- **색은 강조 하나만 쓴다.** 어느 칸인지는 행·열 이름과 "같은 쪽/다른 쪽" 글자가
  말한다 — 색만으로 뜻을 전하지 않는다. 그래서 검증할 범주형 팔레트가 없다
- `eyeHand()`는 **좌우 악력이 같으면 버린다.** 더 센 손을 정할 수 없어서다
- 표 보기(`t`)는 `{id, x, y}` 3열이라 `boardTable()`을 그대로 쓴다

### 번호표를 잃어버렸을 때

**측정 입력 탭에서 번호를 조회하면 `번호표` 단추가 나온다.** 접수 직후 화면에만
재인쇄가 있었는데, 정작 잃어버리는 건 부스를 돌다가다. 이름으로 찾아 다시 뽑는다.

### 자리별 매뉴얼 — `staff.html` 의 매뉴얼 탭

자리 여섯 개의 **임무 · 멘트 · 주의 · 문제 해결**이 `MANUAL` 상수에 들어 있다.
자리를 고르면 그것만 뜨고, 기억해 둔다. 폰에서 서서 보는 화면이다.

### 멘트는 나이대 넷으로 나뉜다

초등 저학년(7\~9) · 초등 고학년(10\~12) · 중·고등(13\~18) · 어른(보호자·성인).
**구간은 `index.html` 의 `AGE_BANDS` 와 같다.** 보드의 또래 기록판과 말이
어긋나면 같은 부스에서 두 가지 이름을 쓰게 된다.

```js
say: ["누구에게나 같은 말"]                    // 배열이면 한 벌
say: { k:[…], h:[…], m:[…], a:[…] }          // 객체면 나이대별
```

- 나이대를 다 적을 필요는 없다. `sayFor()`가 **가장 가까운 나이대** 말을 가져온다.
  빈 칸을 만들지 않으려는 것이다 — 화면이 비면 학생이 즉흥으로 말한다
- 기본값은 **초등 고학년**(`h`)이다. 제일 자주 오는 나이대다
- 나이대를 바꿔도 **"이 말은 하지 않습니다" 네 줄은 그대로다.** 쉽게 말한다고
  "괜찮네요" 가 되면 안 된다. 저학년 멘트가 제일 위험하다
- 저학년 멘트에 **R파·심실 같은 말을 쓰지 않는다.** "뾰족한 거" 로 충분하다
- **저학년이라도 보호자에게는 어른 말로** 한다. 결과지를 들고 가는 건 보호자다
- 지금 166줄이다. 새로 더할 때도 **금지어 검사를 돌릴 것**

**인쇄는 지금 고른 나이대 멘트만** 나간다. 넷을 다 실으면 A4 한 장을 넘는다.
머리글에 "초등 저학년 멘트" 처럼 어느 눈높이인지 적힌다.

**8장(안전·윤리)이 여기에도 그대로 걸린다.** 멘트는 학생이 그대로 읽는 말이라
결과지보다 더 위험하다. 화면 맨 위에 **"이 말은 하지 않습니다"** 상자를 자리와
상관없이 항상 띄운다.

| 하지 않는 말 | 대신 |
|---|---|
| 정상이에요 · 비정상이에요 | 값은 이렇게 나왔어요 |
| 좀 높으시네요 · 낮으시네요 | 오늘은 이 숫자예요 |
| 건강하시네요 · 안 좋으시네요 | 저희는 해석하지 않아요 |
| 병원 가 보세요 | 궁금한 점은 결과지 아래에 적어 뒀어요 |

**"병원 가 보세요"도 금지다.** 좋은 뜻으로 하는 말이지만 그 말 자체가 진단이 된다.

- 멘트에 새 문장을 더할 때는 위 네 줄에 걸리는지 먼저 볼 것.
  브라우저 시험이 **나이대 넷 × 자리 일곱을 전부 훑어** 금지어와 빈 칸을 본다
- **예상 먼저 묻기**(7장)가 심박·악력 멘트 1번에 박혀 있다. 순서를 바꾸지 말 것
- 결과·굿즈 멘트에 **"재미로 계산한 지수"**와 **"교과서 보기 그림"**이 들어 있다.
  4장·7장에서 지우지 말라고 한 문구를 말로도 하게 하는 것이다
- `bold()`가 `**굵게**`만 `<b>`로 바꾼다. 화면과 인쇄 **양쪽**에 걸어야 한다.
  한쪽만 걸면 다른 쪽에 별표가 그대로 찍힌다

**인쇄는 자리마다 A4 한 장**이다. 뽑아서 각 스테이션에 붙이라고 만든 것이다.
`@page` 여백이 11mm라 **쓸 수 있는 높이는 275mm(약 1039px)**다. 297mm가 아니다.
내용을 더하면 뒷장이 거의 빈 채로 딸려 나오므로 브라우저에서 재 볼 것.

**명단에는 찾기 · 정렬 · 거르기가 있다.** 20명이 되면 늘어놓기만 해서는 못 찾는다.
정렬은 이름순(`localeCompare(…, "ko")`) · 근무 많은 순 · 적은 순 · 넣은 순이고,
거르기는 덩어리별 · 배치 없음 · 덩어리 안 고름 · 숨김이다. 셋 다 기억해 둔다.

**찾기는 표만 다시 그린다**(`rosterRows()` + `bindRosterRows()`).
`renderBody()`로 전체를 다시 그리면 글자를 치던 칸에서 **초점이 날아간다.**

**숨기기와 지우기는 다르다.** 숨기기는 교대표를 그대로 두고 목록에서만 빼며
되돌릴 수 있다. 지우기는 **그 사람이 들어간 칸까지 비우고 되돌릴 수 없다** —
몇 시간이 비는지 먼저 알려 주고, 지운 뒤에는 "자리가 비어 있습니다" 경고가
바로 뜬다. 격자에 유령 id가 남지 않게 `S.grid`를 같이 훑는다.

**여럿이 동시에 고치면 마지막이 이긴다.** 문서 하나를 통째로 덮기 때문에
`upd` 로 칸 단위 병합을 하지 않는다 — 섞으면 누가 뭘 지웠는지 알 수 없다.
그래서 **폭이 900px 미만이면 보기 전용**이다. 20명이 동시에 여는 페이지라
대기줄에서 실수로 지우는 것을 막는다.

**서버 저장 실패를 조용히 넘기지 않는다.** `stats` 와 다른 점이다.
순위는 안 나오면 알아채기라도 하는데, 교대표는 고친 게 저장 안 된 걸 모른 채
다음 날 아침에 발견한다. `permission-denied` 면 규칙에 `staff` 가 빠진 것이다.

**이름 말고는 받지 않는다.** 연락처·학번·반은 칸 자체가 없다.
주소가 공개라 누구나 열 수 있다. 지우는 대신 `hide` 로 숨기므로
이미 짜 놓은 교대표가 깨지지 않는다.

### Firestore 규칙
**전체 규칙은 저장소의 `firebase.rules`에 있다.** 세 단계가 한 파일에 들어 있고,
쓸 블록 하나만 남기고 나머지는 주석 처리한다. 아래는 요약이므로 실제 값은
그 파일을 볼 것. 바꾼 뒤 콘솔에서 **게시**를 눌러야 적용된다.

테스트 중에는 전부 열려 있다. **축전 전날 삭제 금지로 바꾸고, 끝나면 잠근다.**

```
// 테스트
match /records/{id} { allow read, write: if true; }
match /stats/{id}   { allow read, write: if true; }

// 실전
match /records/{id} {
  allow read: if true;
  allow create, update: if true;
  allow delete: if false;
}
match /stats/{id} {
  allow read: if true;
  allow create, update: if true;
  allow delete: if false;
}

// 종료 후
match /records/{id} { allow read, write: if false; }
match /stats/{id}   { allow read, write: if false; }
```

**`stats` 규칙을 빠뜨리면 결과 조회의 순위가 전부 "자료를 더 모으는 중"으로 나온다.**
집계 쓰기는 실패해도 조용히 넘어가므로 부스에서 알아채기 어렵다.

`apiKey`가 public 저장소에 올라가는 것은 정상이다. 보안은 규칙이 담당한다.

---

## 8. 안전·윤리 — 코드에서 지켜야 할 선

이 부스는 **의약학 희망 학생들이 운영**한다. 진단으로 읽힐 수 있는 출력은
전부 금지다. PASCO EKG 센서 문서에도 의료기기가 아니며 진단에 쓰면 안 된다고
명시돼 있다.

- 화면·결과지·라벨 어디에도 **"정상 / 비정상" 판정을 넣지 않는다**
- 혈압·심박 수치는 **값만 보여주고 해석하지 않는다**
- "재미로 계산한 지수", "의학적 진단이 아닙니다" 문구를 지우거나 축소하지 않는다
- 이상 수치에 경고 색이나 아이콘을 붙이지 않는다
- 판정 3단계는 생활습관에 대한 것이지 건강 상태가 아니다.
  라벨을 "양호/주의/관리"에서 의학적으로 들리는 말로 바꾸지 말 것

---

## 9. 남은 작업

### 우선순위 높음
1. **라벨 실측 조정** — 62×35mm 시험 인쇄 후 QR 잘림·여백·스캔 성공률 확인

### 실측이 필요한 것
- **결과지 시험 인쇄** — 설정 탭의 "결과지 시험 인쇄". A4 양면, 여백 0, 배율 100%.
  QR 스캔되는지, 파형 모눈이 너무 흐리지 않은지 확인

### 알고 있으나 아직 안 고친 것
- **순위를 전체 참여자 대비로 보여준다.** 어른이 섞인 분포라 초등학생은
  악력·반응속도에서 구조적으로 아래쪽이다. 또래 구간(`AGE_BANDS`)이 이미
  있으므로 `buildStats()`에 구간별 배열을 더하면 "또래 32명 중"으로 바꿀 수 있다
- **`상위 ${100-p}%` 표시가 거꾸로 읽힌다.** 약한 사람에게 "상위 95%"가 뜬다.
  글자만 보면 좋아 보이는데 사실 아래쪽이고, 알아채면 기분이 상한다

### 기획서에 있으나 아직 안 넣은 것
- 문진 마무리 확인란 — "위 내용은 사실과 다름없음을 확인합니다 /
  본 문진은 교육 체험용이며 의학적 진단에 사용되지 않습니다"
- 발목 잡은 항목에 연수 표시 — "화면 시간이 **+2년을** 끌어올렸습니다"
  (지금은 연수 없이 "가장 많이 끌어올렸어요")

### 우선순위 낮음
4. 방문객 데이터 관리 화면 — 고치기·숨기기·미측정 추적. 목록·카운터·CSV·백업은
   이미 기록 탭과 설정 탭에 있다. 남은 것은 **수정·숨김·미측정 추적·백업 가져오기**다.
   지우기는 `allow delete: if false` 에 막히므로 `void` 플래그로 가야 하고,
   그러면 `renderRecords`·`buildStats`·보드·카운터가 **전부** 걸러야 한다
5. 번호표 일괄 인쇄 페이지 (`tickets.html`) — 이름 없이 번호+QR만.
   현재는 보류. 이름이 들어간 라벨을 실시간으로 뽑는 쪽으로 정해졌다

### 미확정 — 사용자에게 물어볼 것
- 문진 문항 최종안 (지금은 초안)
- 연령대별 판정 기준선 세부값
- 주최측 확인 대기: 전기 용량, 임대비품, 체험 안내 공통양식, 야간 부스 존치

---

## 10. 작업 규칙

- **파일을 함부로 늘리지 않는다.** 빌드 도구, 번들러, 프레임워크를 도입하지 않는다.
  세 파일은 각각 그 안에서 자족한다. 학생이 열어서 고칠 수 있어야 하고,
  `index.html`은 오프라인에서 파일 하나로 돌아야 한다.
  **새 파일을 더 만들려면 공유 블록이 늘어나는지 먼저 따질 것.**
  `result.html`은 방문객을 부스 기능에서 떼어 놓으려고 쪼갰고,
  `staff.html`은 검진 데이터와 겹치는 코드가 없어서 값이 쌌다.
  둘 중 어느 쪽도 아닌 파일은 탭으로 붙이는 게 맞다
- **localStorage 저장을 제거하지 않는다.** Firestore가 붙었어도 이중 저장이
  데이터 손실을 막는 유일한 장치다
- 폰에서 쓰므로 **터치 대상은 최소 56px**
- 다크모드 대응 (`prefers-color-scheme` + `data-theme`)
- 문구는 초등학생이 읽을 수 있는 수준으로. 한자어를 줄이고 문장을 짧게
- 커밋 후 GitHub Pages 배포까지 1~2분 걸린다
